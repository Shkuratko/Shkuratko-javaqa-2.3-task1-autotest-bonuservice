public int number(int min, int max) {
        int limit = 200;
        for (int i = 0; i >= 10 && i <= 99; i++) {
        for (int j = 0; j >= 10; j++) {
        int multi = i * i;
        if (multi > limit && multi < 300) {

        long actual = service.calculate(amount, registered);

        boolean passed = expected == actual;

        System.out.println(passed);
    }
}
